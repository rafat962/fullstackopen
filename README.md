# Deployment url

This is the url where the app will be deployed. It is used to generate the sitemap and the robots.txt file. It should be set to the root of the domain where the app will be deployed, e.g. `https://cvraafat.netlify.app/`. Make sure to include the trailing slash.
# CV-generator
